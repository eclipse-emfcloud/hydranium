// GENERATED — do not edit. Run: npm --prefix examples/order-flow/browser run generate
//
// The models from `examples/order-flow/workspace`, keyed by workspace-relative
// path, for the in-memory filesystem the browser host seeds itself from.

import type { FileSystemSeed } from '@hydranium/core';

export const ORDER_FLOW_WORKSPACE_SEED: FileSystemSeed = {
   "commerce-core/internal.domain": "// No `project` header, so this is an\n// ordinary member of the project\n// `money.domain` declares.\n//\n// `AuditStamp` is not `public`: it resolves\n// inside `commerce-core` and must not from\n// `orders`. Adding the modifier silently\n// repairs `audit-leak.domain`, whose job is\n// to fail.\n\nentity AuditStamp {\n   stamp: ID\n   createdBy: String\n   createdAt: String\n}\n",
   "commerce-core/money.domain": "// The shared library project: the `project`\n// header makes this file the folder's\n// descriptor, and every file under\n// `commerce-core/` becomes a member.\n//\n// `public` is what lets another project\n// reach these by their bare name.\n// `internal.domain` deliberately omits it.\n\nproject commerce-core\n\npublic valuetype Money {\n   amount: Number\n   currency: String\n}\n\npublic valuetype ID {\n   value: String\n}\n\npublic entity Address {\n   street: String\n   city: String\n   postalCode: String\n   country: String\n}\n",
   "orders/audit-leak.domain": "// The workspace's ONLY intended error, and\n// it is deliberate.\n//\n// `AuditStamp` is not `public`, so this\n// reference MUST fail to link. Repair it\n// and `hydranium-cli validate` exits zero\n// on this workspace, leaving no failing\n// gate to show.\n\nentity ShipmentLog {\n   stamp: AuditStamp\n}\n",
   "orders/fulfillment.layout": "// Layout for `fulfillment.process`, in its\n// own file: each entry REFERENCES the flow\n// node it positions, so the `.process` file\n// carries no rendering concern and is\n// complete without this one.\n//\n// Dragging a node in the diagram rewrites\n// an entry here.\n//\n// `Cancel` is deliberately absent — a node\n// with no entry is left to client layout,\n// which places it at the ORIGIN, so the\n// entries below start well clear of it.\n//\n// `PaymentOk` deliberately has NO size: a\n// gateway's shape comes from the notation,\n// and pinning one here can only contradict\n// it.\n//\n// A `size` is a MINIMUM; a node grows past\n// it to fit its labels.\n\nlayout FulfillmentLayout for Fulfillment {\n   node Pay at 40, 100 size 160, 60\n   node PaymentOk at 260, 90\n   node Pick at 440, 200 size 160, 60\n   node Ship at 660, 200 size 160, 60\n}\n",
   "orders/fulfillment.process": "// The behavioural half of `orders`. `for\n// Order` and every `writes` / `reads`\n// effect reaches across a GRAMMAR boundary\n// into `orders.domain`.\n//\n// `writes Order.status = PAID` is three\n// references, each scoped by the one\n// before it. Rename `OrderStatus.PAID` and\n// this file follows; delete the field and\n// the effect reports at a precise range.\n\nprocess Fulfillment for Order {\n   task Pay writes Order.status = PAID\n   gateway PaymentOk\n      yes -> Pick\n      no -> Cancel\n   task Pick reads Order.id\n   task Ship writes Order.status = SHIPPED\n   task Cancel writes Order.status = CANCELLED\n   transition Pay -> PaymentOk\n   transition Pick -> Ship\n}\n",
   "orders/orders.domain": "// The consuming project. `requires\n// commerce-core` is what lets the fields\n// below name `ID`, `Money` and `Address`\n// across a project boundary.\n//\n// Nothing here is `public`: a leaf\n// project's declarations only have to reach\n// the `.process` files beside them.\n\nproject orders requires commerce-core\n\nentity Order {\n   id: ID\n   status: OrderStatus\n   total: Money\n   shipTo: Address\n   lines: LineItem[]\n}\n\nenum OrderStatus { NEW, PAID, SHIPPED, CANCELLED }\n\nentity LineItem {\n   sku: ID\n   quantity: Number\n   price: Money\n}\n",
   "orders/returns.process": "// A second process over the same entity, so\n// `.domain` declarations are shared rather\n// than owned by one `.process`. Renaming\n// `Order.status` has to update both.\n\nprocess Returns for Order {\n   task Receive reads Order.id\n   gateway Restockable\n      yes -> Restock\n      no -> Scrap\n   task Restock writes Order.status = NEW\n   task Scrap writes Order.status = CANCELLED\n   transition Receive -> Restockable\n}\n"
};
