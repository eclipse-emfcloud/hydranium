// GENERATED — do not edit. Run: npm --prefix examples/order-flow/browser run generate
//
// The models from `examples/order-flow/workspace`, keyed by workspace-relative
// path, for the in-memory filesystem the browser host seeds itself from.

import type { FileSystemSeed } from '@hydranium/core';

export const ORDER_FLOW_WORKSPACE_SEED: FileSystemSeed = {
   "commerce-core/internal.domain": "// No `project` header, so this file is not a descriptor — it is an ordinary\n// member of the `commerce-core` project declared by `money.domain`.\n//\n// `AuditStamp` carries no `public` modifier, so it exists only at the\n// framework's `project` visibility tier. Inside `commerce-core` it resolves\n// normally (the `stamp: ID` field below reaches `money.domain` across a file\n// boundary within the same project); from `orders` it must not resolve at\n// all, and completion there must not offer it. The server's project-visibility\n// suite asserts that negative — a visibility tier you cannot observe failing is\n// not demonstrated.\n\nentity AuditStamp {\n   stamp: ID\n   createdBy: String\n   createdAt: String\n}\n",
   "commerce-core/money.domain": "// `commerce-core` is the shared library project of this two-project sample\n// workspace. This file carries the `project` header, which is what makes it\n// the folder's project descriptor: `OrderFlowProjectManager` reads the header\n// and every file under `commerce-core/` becomes a member of the project.\n//\n// Every declaration here is marked `public`, so `OrderFlowScopeComputation`\n// emits them at the framework's `public` visibility tier and any project that\n// `requires commerce-core` can reference them by their bare name. Compare\n// `internal.domain`, which deliberately does not.\n\nproject commerce-core\n\npublic valuetype Money {\n   amount: Number\n   currency: String\n}\n\npublic valuetype ID {\n   value: String\n}\n\npublic entity Address {\n   street: String\n   city: String\n   postalCode: String\n   country: String\n}\n",
   "orders/audit-leak.domain": "// Deliberately broken fixture, and the workspace's ONLY intended error.\n//\n// `orders` requires `commerce-core` and therefore resolves that project's\n// `public` declarations. `AuditStamp` is not one of them, so the reference\n// below MUST fail to link. If it ever resolves, the `public` modifier has\n// stopped meaning anything and the whole visibility story in this example is\n// decorative.\n//\n// This is also the file that makes `hydranium-cli validate` exit non-zero on\n// this workspace — repair it and the gate can no longer be shown failing.\n\nentity ShipmentLog {\n   stamp: AuditStamp\n}\n",
   "orders/fulfillment.layout": "// Layout for `fulfillment.process`, in its own file rather than a block inside\n// it. Each entry REFERENCES the flow node it positions, so the `.process` file\n// stays free of rendering concerns and is complete without this one — delete\n// this file and the diagram simply opens unpositioned.\n//\n// The reference crosses a document boundary, which is what makes it interesting:\n// `[FlowNode:ID]` would otherwise resolve against every flow node in the\n// workspace via the global index, so an entry here could bind to a same-named\n// task in an unrelated process and still look correct.\n// `OrderFlowLayoutScopeProvider` narrows the candidates to the nodes of the\n// process named above.\n//\n// This is what `repositionable` / `resizable` are backed by — dragging a node in\n// the diagram rewrites an entry here. Creating one from the canvas writes BOTH\n// files in a single operation, semantics first.\n//\n// `Cancel` is deliberately absent: a flow node with no entry is left to client\n// layout, which is the normal state for anything added in text. That client\n// puts it at the ORIGIN, so the entries below start well clear of it — an\n// unpositioned node dropped on top of a positioned one reads as a rendering\n// fault rather than as the one node this file does not place.\n//\n// The positions run left to right by rank — `Pay`, the gateway, then its two\n// exits — and the gateway's branches split VERTICALLY: `yes` continues down and\n// right to `Pick` and `Ship`, `no` leaves up and left to `Cancel` at the origin.\n// The split is what makes the diamond worth drawing: each branch leaves a\n// different face of it, so `DiamondAnchor` has something to be right about.\n//\n// `PaymentOk` carries a position and NO size, which is the other partial state\n// this file demonstrates: a gateway's shape comes from the notation — see\n// `GATEWAY_LAYOUT_OPTIONS`, where the padding around the name is what sets it —\n// so pinning one here would only be a chance to contradict it. Pinning a height\n// larger than the notation's would also push the gateway's label up under the\n// top vertex, because the micro-layout has already placed it by then.\n//\n// A `size` on the others is a MINIMUM, not a dimension: a node grows past it to\n// fit its labels, so these are floors and the rendered widths are wider.\n\nlayout FulfillmentLayout for Fulfillment {\n   node Pay at 40, 100 size 160, 60\n   node PaymentOk at 260, 90\n   node Pick at 440, 200 size 160, 60\n   node Ship at 660, 200 size 160, 60\n}\n",
   "orders/fulfillment.process": "// The behavioural half of the `orders` project. `for Order` and every\n// `writes` / `reads` effect reaches across a GRAMMAR boundary into\n// `orders.domain`, resolved through the same shared index the LSP head serves.\n//\n// `writes Order.status = PAID` is the seam this example exists for: three\n// references, each scoped by the previous one. `status` is only offered\n// because `Order` resolved, and `PAID` is only offered because `status` is\n// typed `OrderStatus`. Rename `OrderStatus.PAID` in `.domain` and this file\n// follows; delete the field and the effect reports at a precise range.\n\nprocess Fulfillment for Order {\n   task Pay writes Order.status = PAID\n   gateway PaymentOk\n      yes -> Pick\n      no -> Cancel\n   task Pick reads Order.id\n   task Ship writes Order.status = SHIPPED\n   task Cancel writes Order.status = CANCELLED\n   transition Pay -> PaymentOk\n   transition Pick -> Ship\n}\n",
   "orders/orders.domain": "// `orders` is the consuming project. `requires commerce-core` populates\n// `Project.dependencies`, which the framework's project-scope filter walks —\n// that is what lets the fields below reference `ID`, `Money` and `Address`\n// across a project boundary by their bare names.\n//\n// Nothing here is `public`: `orders` is a leaf project, so its declarations\n// only need to be visible to the `.process` files beside them, which is\n// exactly what the `project` tier gives them.\n\nproject orders requires commerce-core\n\nentity Order {\n   id: ID\n   status: OrderStatus\n   total: Money\n   shipTo: Address\n   lines: LineItem[]\n}\n\nenum OrderStatus { NEW, PAID, SHIPPED, CANCELLED }\n\nentity LineItem {\n   sku: ID\n   quantity: Number\n   price: Money\n}\n",
   "orders/returns.process": "// A second process over the same domain entity, so the workspace shows that\n// `.domain` declarations are shared by several `.process` files rather than\n// owned by one. Renaming `Order.status` has to update both.\n\nprocess Returns for Order {\n   task Receive reads Order.id\n   gateway Restockable\n      yes -> Restock\n      no -> Scrap\n   task Restock writes Order.status = NEW\n   task Scrap writes Order.status = CANCELLED\n   transition Receive -> Restockable\n}\n"
};
